<%= form_with(model: ward) do |form| %>
  <% if ward.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(ward.errors.count, "error") %> prohibited this ward from being saved:</h2>

      <ul>
        <% ward.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :ward_name, style: "display: block" %>
    <%= form.text_field :ward_name %>
  </div>

  <div>
    <%= form.label :division_id, style: "display: block" %>
    <%= form.text_field :division_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
